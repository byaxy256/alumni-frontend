{
  "installCommand": "npm --prefix 'Alumni frontend' install",
  "buildCommand": "npm --prefix 'Alumni frontend' run build",
  "outputDirectory": "Alumni frontend/build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
