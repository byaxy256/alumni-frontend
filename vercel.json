{
  "buildCommand": "cd 'Alumni frontend' && npm install && npm run build",
  "outputDirectory": "Alumni frontend/build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
