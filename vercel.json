{
  "installCommand": "cd 'Alumni frontend' && npm install",
  "buildCommand": "cd 'Alumni frontend' && npm run build",
  "outputDirectory": "Alumni frontend/build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
