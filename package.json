{
  "scripts": {
    "build": "npm --prefix 'Alumni frontend' run build",
    "install:frontend": "npm --prefix 'Alumni frontend' install"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "sonner": "^2.0.7"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  }
}
